// TODO : remove file
const state = {
  // Initial state of group-store
}

const getters = {
  // Getters to access group-store values
}

const actions = {
  // Asynchronous mutations commits to modify group-store
}

const mutations = {
  // Synchronous modifications of group-store
}

export default {
  state,
  getters,
  actions,
  mutations
}
